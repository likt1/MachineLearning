%% kernel(datapoint1, datapoint2)
% Calculates the kernel between arrays 'datapoint1' and 'datapoint2'
% For this assignemnt this is given to be the dot product between the two
%%
function out = kernel(a, b)
    out = dot(a, b);
end